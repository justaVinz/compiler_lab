#include "TokenizeHelper.h"

#include "structs/TokenizeAttempt.h"

static TokenizeAttempt tokenizeStringIdentifiers(std::string code) {
    return TokenizeAttempt();
}

static TokenizeAttempt tokenizeKeywordPunctuators(std::string code) {
    return TokenizeAttempt();
}

